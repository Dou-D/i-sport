import UserForm from '@/components/UserForm';
import { PageContainer } from '@ant-design/pro-components';

export default () => {
  return (
    <PageContainer ghost>
      <UserForm />
    </PageContainer>
  );
};

export interface UserFormType {
  // 运动习惯
  sport_accustomed: string;
  // 出生日期 -> 时间戳
  birthTime: number;
  // 身高
  height: string;
  // 体重
  weight: string;
  // 运动强度
  sport_intensity: string;
  // 运动频率
  sport_frequency: string;
  // 单次运动时间
  sport_duration: string;
}
